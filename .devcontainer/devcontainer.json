{
  "name": "dah2026",
  "image": "node:24-alpine",
  "workspaceMount": "source=${localWorkspaceFolder},target=/app,type=bind",
  "mounts": [
    "source=${localEnv:HOME}/.gitconfig,target=/root/.gitconfig,type=bind",
    "source=dah2026-node-modules,target=/app/node_modules,type=volume",
    "source=dah2026-angular-cache,target=/app/.angular,type=volume"
  ],
  "appPort": [8100],
  "workspaceFolder": "/app",
  "remoteUser": "root",
  "postCreateCommand": "apk add --no-cache git bash && npm i -g npm@latest && npm ci --prefix /app && git -C /app -c safe.directory=/app config url.\"https://github.com/\".insteadOf \"git@github.com:\"",
  "postStartCommand": "/bin/bash -i -c 'npm run start'",
  "shutdownAction": "none",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      },
      "extensions": [
        "github.copilot-chat",
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint",
        "christian-kohler.path-intellisense",
        "Webnative.webnative",
        "angular.ng-template",
        "echoapi.echoapi-for-vscode",
        "mikestead.dotenv"
      ]
    }
  }
}